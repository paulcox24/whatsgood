$(".feed").append("<%= j render(@activities) %>");
<% if @activities.next_page %>
$('.pagination').replaceWith('<%= j will_paginate(@activities) %>');
<% else %>
$('.pagination').remove();
<% end %>
$("#profile-feed-can-load-more").val("true");