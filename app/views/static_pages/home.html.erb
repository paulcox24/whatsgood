<div class="location"><h1>whatsgood</h1></div>
<div class="location-data"></div>

<% @events.each do |event| %>
	<div>
		<h1><%= event['title'] %></h1>
		<p><%= sanitize event['description'] %></p>
		<%= link_to 'Link', event['url'] %>
	</div>
	<hr>
<% end %>
